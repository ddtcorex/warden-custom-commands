#!/usr/bin/env bash
[[ ! ${WARDEN_DIR} ]] && >&2 echo -e "\033[31mThis script is not intended to be run directly!\033[0m" && exit 1

WARDEN_USAGE=$(cat <<EOF
Fix Magento dependency versions in .env file based on your Magento version.

\033[33mUsage:\033[0m
  fix-deps [-h|--help] [--dry-run] [-v|--version=<version>]

\033[33mOptions:\033[0m
  -h, --help                Display this help menu

\033[33mArguments:\033[0m
  --dry-run                 Preview changes without modifying .env

  -v, --version=VERSION     Specify Magento version manually (e.g., 2.4.8)
                            Auto-detects from META_VERSION, composer.json, or installation

\033[33mDependencies Updated:\033[0m
  PHP_VERSION, MYSQL_DISTRIBUTION, MYSQL_DISTRIBUTION_VERSION,
  OPENSEARCH_VERSION/ELASTICSEARCH_VERSION, REDIS_VERSION,
  COMPOSER_VERSION, RABBITMQ_VERSION, VARNISH_VERSION

\033[33mExamples:\033[0m
  warden fix-deps --dry-run                # Preview changes
  warden fix-deps -v=2.4.8                 # Apply fixes for Magento 2.4.8

\033[33mNote:\033[0m
  Run with --dry-run first to review changes. After updating, run
  'warden env down' and 'warden env up' to rebuild containers.

EOF
)
