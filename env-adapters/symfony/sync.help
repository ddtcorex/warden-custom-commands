#!/usr/bin/env bash
printf "\033[33mSymfony Sync Options:\033[0m
  --file           Sync code excluding vendor, node_modules, and var/cache/log
  --media          Sync public/uploads directory
  --db             Stream database dump from remote to local
  --full           Sync all of the above
  --dry-run        RSYNC dry-run mode
"
