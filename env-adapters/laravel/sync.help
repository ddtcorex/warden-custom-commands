#!/usr/bin/env bash
printf "\033[33mLaravel Sync Options:\033[0m
  --file           Sync code excluding vendor, node_modules, and framework logs/cache
  --media          Sync storage/app/public directory
  --db             Stream database dump from remote to local
  --full           Sync all of the above
  --dry-run        RSYNC dry-run mode
"
