#!/usr/bin/env bash
printf "\033[33mWordPress Sync Options:\033[0m
  --file           Sync code excluding wp-content/uploads and cache
  --media          Sync wp-content/uploads directory
  --db             Stream database dump from remote to local
  --full           Sync all of the above
  --dry-run        RSYNC dry-run mode
"
